import React from 'react'
import classes from './aboutSection.module.scss'

const AboutSection = () => {
	return (
		<section className={classes.about}>
			<h2 className={classes.heading}>what is pumpfit?</h2>
		</section>
	)
}

export default AboutSection
